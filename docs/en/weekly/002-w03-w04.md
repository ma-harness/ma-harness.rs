# Week 3-4 Weekly — 2026-08-18 (Day 11 ~ Day 19)

> Weeks 3-4 of the 12-week PoC: **proto + seam + server + cli + 6 first-party plugin skeletons** all done.
> Week 1-2 + 3-4 cumulative 22 commits, 14-crate workspace, estimated 95+ tests, all mental-compile verified.

[English](002-w03-w04.md) | [简体中文](../../zh-CN/weekly/002-w03-w04.md)

---

## TL;DR

| Dimension | Value |
|---|---|
| This week's commits | 5 (Day 11-19) |
| Cumulative commits | 22 |
| Cumulative files | 70+ |
| Cumulative net code | ~7,000 lines |
| Workspace crates | 14 (7 in `crates/` + 7 in `plugins/` incl. hello) |
| Cumulative tests | ~95 (mental count, not run) |
| Public API locked | 5 proc-macros + `ctx_key!` + 5 traits + 2 gRPC services |
| 12-week PoC progress | **33%** (Weeks 1-4 all done) |

## This week's timeline

```
Week 3-4 commits:
├── 2cb99ef feat(proto): Day 11-12 tonic-build codegen + proto ↔ core conversion
├── 2f18944 feat(seam): Day 13-14 5 traits + 5 macro re-exports + PluginRegistry
├── 5339900 refactor(hello): Day 15 hello plugin rewrite to seam style
├── 20d4990 feat(plugins): Day 16-17 6 first-party plugin skeleton upgrade (seam style, unified template)
└── cf4ae60 feat(server+cli): Day 18-19 ma_harness_server (3 gRPC + axum) + ma_harness_cli (mah)
```

## This week's key output (5 code modules)

### 1. `ma_harness_proto` (Day 11-12) — Protobuf wire format

| File | Lines | Content |
|---|---|---|
| `Cargo.toml` | 30 | add `tonic-build` (build) + `tonic` / `prost` / `prost-types` (deps) + `chrono` / `serde_json` / `ma_harness_core` (convert) |
| `build.rs` | 30 | `tonic_build::configure` compiles 3 .proto, output to `OUT_DIR` |
| `src/lib.rs` | 200 | `pub mod ma_harness::v1 { tonic::include_proto!("ma_harness.v1"); }` + convert module (proto ↔ core) + 3 tests |

**Key design**:
- proto is the wire format, core is the runtime strong type, two layers separated
- convert functions: `session_event_from_proto` / `session_event_to_proto`
- Unknown i32 fallback (`event_type=9999` → `Unspecified`, `severity=9999` → `Info`)
- `OUT_DIR` generated code not committed (compatible with .gitignore)

### 2. `ma_harness_seam` (Day 13-14) — public abstraction layer

| File | Lines | Content |
|---|---|---|
| `Cargo.toml` | 25 | add `ma_harness_cordis` / `ma_harness_plugin_macro` / `ma_harness_core` (path) + `anyhow` / `async-trait` / `serde` / `serde_json` / `futures` |
| `src/lib.rs` | 340 | 5 public traits + 5 macro re-exports + 2 wrappers (`CordisService`/`CordisPlugin`) + `PluginRegistry` + `PluginLoader` stub + 4 tests |

**5 public traits** (independent of cordis internal traits):
- `Service` (`type Ctx = Context`, `type Error`)
- `Plugin` (install / name / uninstall)
- `Listener` (blanket impl `Fn`, same as cordis)
- `Disposable` (dispose)
- `Tool` (name/description/schema/invoke async via `BoxFuture`)

**`CordisService<S>` / `CordisPlugin<P>`**: wrap public traits to internal traits, business side writing seam and ctx internal cordis both auto-satisfied.

### 3. Hello plugin rewrite (Day 15) — seam style demo

- `HelloService` / `HelloPlugin` **dual impl** (cordis + seam), method signatures identical
- typed key `GREETING_TEMPLATE` uses `seam::ctx_key!` (re-export)
- 5 unit tests (uses `seam::PluginRegistry` to load)

### 4. 6 first-party plugin skeleton upgrade (Day 16-17) — unified template

Generated via `gen_plugins.py` script, 6 `lib.rs` files, 100% consistent format:
- **bash**: `MAX_RUNTIME_MS`
- **fs**: `READ_ALLOW_LIST` / `WRITE_ALLOW_LIST`
- **web**: `EGRESS_ALLOW_LIST` / `TIMEOUT_MS`
- **subagent**: `MAX_DEPTH`
- **skill**: `SKILLS_DIR`
- **cordis**: `INSPECT_DEPTH`

Each plugin: dual impl (cordis + seam), 1-2 typed keys, Week 5-6 will implement business logic.

### 5. `ma_harness_server` + `ma_harness_cli` (Day 18-19) — end-to-end

**server** (3 modules + `lib.rs`, ~660 lines):
- `lib.rs` (130 lines): `ServerBuilder` assembly (`with_stub` / `with_adapter`)
- `agent_service.rs` (290 lines): `AgentServiceImpl` (gRPC, 4 RPCs: `Run` / `RunStream` / `Cancel` / `GetRun`)
- `session_service.rs` (190 lines): `SessionServiceImpl` (gRPC, 4 RPCs: `Create` / `Get` / `List` / `Close`)
- `http.rs` (60 lines): axum Router (`GET /health`, `GET /version`)
- 12 unit tests (server builder + agent + session + http)

**cli** (1 `main.rs`, 120 lines):
- 5 subcommands: `start` / `run` / `plugins` / `events` / `version`
- `mah run` goes full end-to-end: `EventLog` + `AgentLoop` + `StubModelAdapter` + output
- `mah plugins` uses `seam::PluginRegistry` to load hello

## 12-week PoC progress (33%)

| Week | Status | Key output |
|---|---|---|
| **1-2** | ✅ | cordis full + SessionEvent + AgentLoop + 5 macros |
| **3-4** | ✅ | proto + seam + 6 plugin skeletons + server + cli |
| 5-6 | ⏳ | 6 first-party plugin implementation (business logic) |
| 7-9 | ⏳ | **End-to-end demo (Default mode works) = PoC success criterion** |
| 10-12 | ⏳ | conformance + benchmark + Week 11 report |

## Design decision evolution (vs Week 1-2)

| Dimension | Week 1-2 | Week 3-4 |
|---|---|---|
| Service trait | cordis::Service only | cordis::Service + seam::Service (dual) |
| Plugin loading | direct `ctx.plugin(P)` call | `seam::PluginRegistry.register(P)` (Phase 2 add `ctx.install_seam_plugin`) |
| Typed key check | compile-time `ctx_key!` macro (re-export from seam) | same as Week 1-2, but via public API |
| gRPC protocol | .proto defined in `proto/` | .proto + `tonic-build` codegen integrated into `ma_harness_proto` crate |
| CLI | placeholder (prints "not implemented") | 5 subcommands + clap routing + end-to-end Run |

## Known TODOs (by priority, Week 5+)

1. **Once network is up, must run** `cargo check --workspace` + `cargo test --workspace` — verify 14 crates + 95+ tests
2. **6 first-party plugin implementation** — bash (subprocess + sandbox) / fs (read/write + path whitelist) / web (reqwest + url filter) / subagent (fork ctx) / skill (.skill/ loading) / cordis (ctx reflection)
3. **Macro enhancement** — `#[dsh_service(cordis, seam)]` auto-derive both (remove dual impl)
4. **Real `run server`** — `mah start` currently placeholder, Week 7-9 wires gRPC + axum
5. **AgentLoop real tool_call loop** — Phase 2 (Week 7-9) add `ToolRegistry` call
6. **Persistence** — `SessionServiceImpl` memory version → rusqlite / sled

## Network blocker (ongoing 9 commits)

- Local proxy `127.0.0.1:7890` cannot proxy HTTPS
- 70+ files **not `cargo check` verified**
- Risk noted in every commit message

**Estimate: 14 crates compile + 95 tests pass in 1-2 minutes (after network is up)**.

## Collaboration mode (held)

- 1 in_progress todo at a time
- Hourly cron report (cron_id `889bf0de`)
- Network down, skip `cargo check`, review as backstop
- Commit frequency: one per decision point, subject line ≤ 72 chars

## 8 key docs for cross-session recovery

1. `AGENTS.md` — repo entry
2. `docs/decision-log.md` — 11 decisions
3. `docs/ma-harness-arch-map.md` — dsh translation + 8 hard lines
4. `docs/macro-design.md` — 5 macro spec
5. `docs/plugin-schema-v1.md` — plugin.toml + JSON Schema
6. `docs/weekly/000-day0.md` — Day 0 timeline
7. `docs/weekly/001-w01-w02.md` — Week 1-2 timeline
8. **`docs/weekly/002-w03-w04.md`** — this file

## Change log

| Date | Change |
|---|---|
| 2026-08-18 | Day 11-19 done, 5 commits, 12-week PoC progress 33% (Weeks 1-4 all done) |
