# Week 1-2 周报 — 2026-08-18 (Day 0 ~ Day 9)

> 12 周 PoC 的前 9 天, **cordis 元框架 + core (SessionEvent/AgentLoop) + macro 完整骨架**完成。
> 累计 16 commit, 14 crate workspace, 估计 75+ 个测试, 全部 mental compile 验证, 无 cargo check。

---

## TL;DR

| 维度 | 数值 |
|---|---|
| Commit | 16 (Day 0 ~ Day 9) |
| 文件 | 60+ |
| 净代码 | ~5500 行 (含 spec / 注释 / 测试) |
| Workspace crate | 14 (8 crates/ + 6 plugins/, +1 hello plugin) |
| 单元/集成测试 | ~75 (mental count, 未跑) |
| 公开 API 锁定 | 5 proc-macro + ctx_key! + Service / Plugin / Listener / Disposable traits |
| 内部 crate | cordis / core / server (API 频繁变, 不上锁) |

## 时间线 (按 commit)

```
Day 0 (起步 + spec):
├── 27d4159 docs: 项目宪章 (AGENTS.md + decision-log + code-mode-deferred + tech-stack)
├── 1ea6cb0 chore: 加 .gitignore
├── 12229d8 docs(arch): 机制映射表
├── 5f733ee docs(macro): 5 个 proc-macro
├── 69cbcad docs(schema): plugin.schema.json v1
├── 8d4e527 feat(proto): 3 个 .proto
├── 080fb1d docs: crate 公开性同步
├── 362200e feat(workspace): 13 个空 crate
└── 6ae6e52 docs(weekly): Day 0 总结

Day 1-2 (cordis 骨架):
└── 9434b78 feat(cordis): Context / Service / Plugin / typed key (13 测试)

Day 3 (extend + ctx_key!):
└── 257aa79 feat(cordis+macro): extend / is_snake_case / ctx_key! (13 测试)

Day 4 (hello plugin):
└── e9d94b6 feat(hello-plugin): 端到端 demo (11 测试)

Day 5 (cordis 完整):
└── 085eaa5 feat(cordis): listener / scope / fork / dispose (18 测试)

Day 6-7 (SessionEvent + 日志):
└── f55b0b3 feat(core): SessionEvent 类型 + rusqlite append-only (17 测试)

Day 8 (agent loop):
└── 508d537 feat(core): AgentLoop + ModelAdapter + StubModelAdapter (3 测试)

Day 9 (5 个 macro + ToolRegistry):
└── 4a66489 feat(macro+core): 5 个 proc-macro 真实实现 + ToolRegistry (10 测试)
```

## 关键设计落档 (5 份 spec + 1 份 weekly)

| 文档 | 状态 | 关键决策 |
|---|---|---|
| `AGENTS.md` | ✅ 锁 | 仓库结构 / 命名 / 跟 dsh 划清 / 协作模式 |
| `docs/decision-log.md` | ✅ 锁 | 命名 / 范围 / ctx 规范 / crate 公开性矩阵 |
| `docs/code-mode-deferred.md` | ✅ 锁 | Code Mode 推到 Phase 2 (三方案备选) |
| `docs/tech-stack.md` | ✅ 锁 | 14 节 crate 冻结 + "不引入"清单 |
| `docs/ma-harness-arch-map.md` | ✅ 锁 | 12 节 dsh 翻译 + 8 条硬线 |
| `docs/macro-design.md` | ✅ 锁 | 5 个 proc-macro 完整规范 |
| `docs/plugin-schema-v1.md` | ✅ 锁 | plugin.toml + JSON Schema v1 + 6 first-party 例子 |
| `proto/ma_harness/v1/{agent,session,event}.proto` | ✅ 锁 | 25 message + 3 service |

## 代码产出 (8 份 spec + 7 份代码)

### Cordis 元框架 (crates/ma_harness_cordis, 7 文件 ~1600 行)

| 文件 | 行数 | 内容 |
|---|---|---|
| `lib.rs` | 30 | re-export |
| `context.rs` | 750 | Context 主结构 + typed key + service + plugin + extend_from + on/emit/scope/dispose/fork |
| `key.rs` | 200 | CtxKey<T> + is_snake_case const fn + check_or_panic |
| `service.rs` | 70 | Service trait |
| `plugin.rs` | 150 | Plugin trait + PluginRegistry |
| `listener.rs` | 270 | ListenerEvent + Listener + ListenerRegistry (RwLock) + 5 测试 |
| `disposable.rs` | 280 | Disposable + Scope + DisposableEntry (LIFO, 幂等) + 5 测试 |

**API**:
- `Context::new` / `default` / `fork` / `dispose` / `is_disposed`
- `set<T>` / `get<T>` / `get_ref<T>` / `remove<T>` / `contains<T>` (typed key)
- `inject<S>` / `inject_from<S>` / `service<S>` / `service_or<S>` (强类型 service)
- `plugin<P>` / `uninstall_plugin` / `plugins` / `plugin_by_name`
- `extend_from` (parent service 引用, 不继承 key/plugin)
- `on<E, L>` / `emit<E>` / `listener_count<E>` (强类型事件, reentrancy guard)
- `scope` / `on_dispose` (RAII 资源管理)

### Core (crates/ma_harness_core, 4 文件 ~1500 行)

| 文件 | 行数 | 内容 |
|---|---|---|
| `lib.rs` | 20 | re-export |
| `event.rs` | 440 | EventType (15 variant) + Severity + SessionEvent + validate (3 不变量) |
| `log.rs` | 570 | EventLog (rusqlite) + EventQuery + EventPage + StoredEvent |
| `agent.rs` | 340 | ModelRequest/Response/Message + ModelAdapter trait + StubModelAdapter + AgentLoop + AgentRunRequest/Response |
| `tool.rs` | 200 | ToolSchema + ToolInvokeFn + ToolEntry + ToolRegistry |

**API**:
- `EventLog::open` / `open_in_memory` / `append` / `query` / `get_model_visible` / `count`
- `SessionEvent::new` + builder (with_severity / with_run_id / with_plugin / with_payload / with_error) + `validate`
- `AgentLoop::new` / `with_stub` / `with_ctx` / `run` (async)
- `ModelAdapter` trait + `StubModelAdapter`
- `ToolRegistry::new` / `register` / `invoke` (async) / `list_schemas` / `count`

### Plugin Macro (crates/ma_harness_plugin_macro, 3 文件 ~1200 行)

| 文件 | 行数 | 内容 |
|---|---|---|
| `lib.rs` | 1100 | 5 个 proc-macro 真实实现 + ctx_key! macro_rules! |
| `tests/macros_compile.rs` | 160 | 6 个集成测试 |
| `tests/ctx_key.rs` | 4 | 空 (跟 macros_compile 合并) |

**5 个 macro** (公开 API 锁):
- `#[dsh_service]` (derive) — 自动 impl Service trait
- `#[dsh_listener]` (derive) + `#[dsh_listener::on(...)]` (attribute) — 标记 listener
- `#[dsh_tool]` (attribute) — 提取 schema + 生成 invoke
- `#[dsh_command]` (attribute) — clap 集成
- `#[dsh_handler]` (attribute) — model adapter (Phase 1 透传)
- `ctx_key!()` (macro_rules!) — 编译期 snake_case 校验

### Hello Plugin (plugins/ma_harness_plugin_hello, 4 文件)

完整 demo: ctx inject service + typed key + plugin install,演示 ctx 是"活的"DI 容器。

### 6 个 First-Party 插件骨架 (Phase 2 实现)

`bash / fs / web / subagent / skill / cordis` — 各 3 文件 (Cargo.toml + lib.rs + plugin.toml) 占位。

## 12 周 PoC 进度 (15%)

| Week | 状态 | 关键产出 |
|---|---|---|
| **1-2** | ✅ | cordis 完整 + SessionEvent + AgentLoop + 5 macro |
| 3-4 | ⏳ | proc-macro 完整 schemars + 6 first-party 插件骨架 |
| 5-6 | ⏳ | 6 个 first-party 插件实现 |
| 7-9 | ⏳ | **端到端 demo (Default 模式跑通) = PoC 成功判据** |
| 10-12 | ⏳ | conformance + benchmark + Week 11 报告 |

## 网络阻塞 (持续)

- 本机代理 `127.0.0.1:7890` 不能代理 HTTPS
- 所有 60+ 文件 **未 cargo check 验证**
- 风险已 5 次在 commit message 标注

**预计 14 个 crate 编译 + 75 个测试跑通需要 1-2 分钟 (网络通后)**。

## 已知 TODO (按优先级)

1. **网络通后**:`cargo check --workspace` → 修编译错 → `cargo test --workspace` → 修测试错
2. `ma_harness_proto` 加 tonic-build + 3 个 .proto codegen
3. `ma_harness_seam` 加 5 个 macro re-export + Plugin 公开抽象
4. 6 个 first-party 插件实装 (bash / fs / web / subagent / skill / cordis)
5. `ma_harness_server` axum + tonic 拼装
6. `ma_harness_cli` clap 入口

## 决策记忆 (跨 session 恢复用)

如果新 session / 明天接着干, 重点读:

1. `AGENTS.md` — 仓库入口
2. `docs/decision-log.md` — 11 项决策
3. `docs/ma-harness-arch-map.md` — 跟 dsh 的翻译 + 8 条硬线
4. `docs/macro-design.md` — 5 个 macro 规范
5. `docs/plugin-schema-v1.md` — plugin.toml + JSON Schema
6. `docs/weekly/000-day0.md` — Day 0 timeline
7. `docs/weekly/001-w01-w02.md` — 本文件 (Week 1-2 timeline)

## 协作模式 (确认)

- 每次 1 个 in_progress todo
- 每 1h cron 汇报 (cron_id `889bf0de`)
- 网络不通, 跳过 cargo check, review 兜底
- commit 频率: 每个决策点一个, 主题行 ≤ 72 字符

## 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-18 | Day 0 ~ Day 9 完整收官, 16 commit, 75 测试, 12 周 PoC 进度 15% |
