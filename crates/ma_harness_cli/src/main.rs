//! ma-harness CLI 入口
//!
//! 二进制名: `mah`
//! Week 7-9 加 clap 解析 + 路由到 `ma_harness_server`.

#![deny(unsafe_code)]
#![warn(missing_docs)]

fn main() {
    eprintln!("ma-harness CLI 尚未实现. 预计 Week 7-9 起.");
    std::process::exit(1);
}
